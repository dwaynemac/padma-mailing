<% if @contact && !@contact.email.blank?%>
  <%= render partial: "send_to_contact", locals: {template: template} %>
<% else %>
  <%= render partial: "send_to_mail", locals: {template: template} %>
<% end %>
