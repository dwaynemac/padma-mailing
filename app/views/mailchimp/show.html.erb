
<div>
    <legend>Elegí tus segmentos</legend>

    <legend>nuevo segmento</legend>

    <div class="btn-group">
       <button type="button" class="btn btn-default">prospect</button>
       <button type="button" class="btn btn-default">exalumnos</button>
       <button type="button" class="btn btn-default">alumnos</button>
     </div>

    <div class="btn-group">
       <button type="button" class="btn btn-default">solo hombres</button>
     </div>
    
    <div class="btn-group">
       <button type="button" class="btn btn-default">todos</button>
       <button type="button" class="btn btn-default">fp</button>
       <button type="button" class="btn btn-default">perfil (include p y p+)</button>
       <button type="button" class="btn btn-default">solo pmas</button>
     </div>
</div>

<%= form_for @mailchimp, url: mailchimp_path, method: 'PUT' do |f|  %>  
  <fieldset>
    <legend><%= t('mailchimp.show.list_syncronization') %></legend>

    <label for="students_list_id"><%= t('mailchimp.show.list_on_mailchimp_for.students') %></label>
    <%= f.select :students_list_id, @mailchimp_lists.map{|l| [l["name"],l["id"]]} %>
    <% unless @mailchimp.students_list_id.blank? -%>
      <%= link_to 'Check', check_mailchimp_path(list_sys_name: 'students'), class: 'btn btn-mini' %>
    <% end -%>

    <label for="all_list_id"><%= t('mailchimp.show.list_on_mailchimp_for.all') %></label>
    <%= f.select :all_list_id, @mailchimp_lists.map{|l| [l["name"],l["id"]]} %>
    <% unless @mailchimp.all_list_id.blank? -%>
      <%= link_to 'Check', check_mailchimp_path(list_sys_name: 'all'), class: 'btn btn-mini' %>
    <% end -%>

    <label for="p_former_students_list_id"><%= t('mailchimp.show.list_on_mailchimp_for.p_former_students') %></label>
    <%= f.select :p_former_students_list_id, @mailchimp_lists.map{|l| [l["name"],l["id"]]} %>
    <% unless @mailchimp.p_former_students_list_id.blank? -%>
      <%= link_to 'Check', check_mailchimp_path(list_sys_name: 'p_former_students'), class: 'btn btn-mini' %>
    <% end -%>
    
  </fieldset>
  
  <%= f.submit nil, class: 'btn btn-primary' %>
<% end -%>
<%= link_to 'Reset integration', mailchimp_path, method: :delete, confirm: 'sure?', class: 'btn btn-danger btn-mini'  %>
