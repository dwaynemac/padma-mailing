module UXEventsHelper

  def register_ux_event(event_name, options={})
    true
    #    intercom_options = {
    #  event_name: event_name,
    #  created_at: Time.now.to_i,
    #  user_id: current_user.username
    #}
    #if options[:metadata]
    #  intercom_options = intercom_options.merge({
    #    metadata: options[:metadata]
    #  })
    #end
  end
end
